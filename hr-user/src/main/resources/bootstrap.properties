spring.application.name=hr-user

# configura��o da url do config server
spring.cloud.config.uri=http://hr-config-server:8888

# com essa configura��o alteramos o perfil do microservi�o
spring.profiles.active=prod

# configura��o para que o actuator atualize a config em runtime
management.endpoints.web.exposure.include=*